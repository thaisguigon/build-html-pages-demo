{
  "name": "technical-test",
  "version": "1.0.0",
  "workspaces": [
    "./backend",
    "./frontend",
    "./shared"
  ],
  "scripts": {
    "compose:up": "docker compose -f 'compose.yaml' up -d --build",
    "compose:down": "docker compose down"
  }
}
